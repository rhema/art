vector() // array that you can dynamically resize


//declare the array
color[] colorarray= new color[12];
//creat the array with the keyword new and define the length

//assign values to each element

float indirectness;
//int[] colorarray = new colorarray[12];

indirectness (0, 1);

//random (0,3)

colorlookup= indirectness*colorarray.length;
//colorlookup = indirect*colorarray.size, if we use arraylist()

for( i=0, i < colorlookup; i++)
{

  currentcolor = colorarray[(int) random(0, colorlookup)];
  fill(currrentcolor, 100, 100);

  flower(x, y, size, size);
  
}

//practicing below this line

ArrayList colorarray;

void setup(){
  colorarray = new ArrayList();}
  

colorarray.get(i)

